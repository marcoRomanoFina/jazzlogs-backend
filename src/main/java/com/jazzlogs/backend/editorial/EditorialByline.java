package com.jazzlogs.backend.editorial;

// Who's credited as having written this editorial — the same narrator
// characters as Series/Playlist's voice, plus JAZZLOGS for the outlet's own
// unsigned pieces (the default — see EditorialService's upsert methods).
public enum EditorialByline {
    MARK,
    LAURA,
    ALICE,
    ADAM,
    JAMES,
    ALLIE,
    BOB,
    NATALIE,
    JAZZLOGS
}
